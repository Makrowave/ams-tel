export enum Statuses {
  unAssembled = 1,
  assembled,
  sold,
  delivery,
  prepaid,
  warranty,
}
//Move this one to config?
//This one should be changed depending on business layout
export enum Places {
  shop1 = 1,
  shop2,
  shop3,
  storage1,
  storage2,
  storage3,
}
